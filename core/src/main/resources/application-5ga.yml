systems:
  configs:
    SYSTEM_5GA:
      datasources:
        PRIMARY:
          url: jdbc:mysql://localhost:3306/pm_access?useSSL=false&serverTimezone=UTC
          username: root
          password: nam123
          driverClassName: com.mysql.cj.jdbc.Driver
          maximumPoolSize: 15
          connectionTimeoutMs: 60000
        CLICKHOUSE:
          url: jdbc:clickhouse://localhost:8123/pm_api_5gaccess
          username: default
          password: nam123
          driverClassName: com.clickhouse.jdbc.ClickHouseDriver
          maximumPoolSize: 10
          connectionTimeoutMs: 60000
      primaryDatasource: PRIMARY
      deploy: true
      custom:
        isUsingClickhouse: true
        isUsingMySQL: true
        consumerThreads: 50
        clickHouseDatabase: pm_access_5g
        isAutoAddingColumn: false
        serviceName: 5GA
